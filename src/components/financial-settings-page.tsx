"use client";

import { CirclePlus, CreditCard, LoaderCircle, Menu, Pencil, Save, Trash2, WalletCards, X } from "lucide-react";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { AppSidebar } from "@/components/app-sidebar";

type Account={id:string;name:string;type:"CAIXA"|"BANCO"|"CARTEIRA"|"OUTRA";openingBalance:number;currentBalance:number;active:boolean};
type FeeRule={minimumInstallments:number;maximumInstallments:number|null;feePercent:number};
type Method={id:string;code?:string;name:string;supportsInstallments:boolean;variableFee:boolean;defaultFeePercent:number;defaultAccountId?:string;active:boolean;rules:FeeRule[]};
type Editing={entity:"account"|"method";id?:string};
const accountLabels={CAIXA:"Caixa",BANCO:"Banco",CARTEIRA:"Carteira digital",OUTRA:"Outra"};
const money=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"});
const blankRule=():FeeRule=>({minimumInstallments:1,maximumInstallments:1,feePercent:0});

export function FinancialSettingsPage(){
  const [accounts,setAccounts]=useState<Account[]>([]);const [methods,setMethods]=useState<Method[]>([]);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState("");const [notice,setNotice]=useState("");const [mobileMenu,setMobileMenu]=useState(false);const [editing,setEditing]=useState<Editing>();
  const [name,setName]=useState("");const [accountType,setAccountType]=useState<Account["type"]>("BANCO");const [openingBalance,setOpeningBalance]=useState("0");const [active,setActive]=useState(true);const [supportsInstallments,setSupportsInstallments]=useState(false);const [variableFee,setVariableFee]=useState(false);const [defaultFee,setDefaultFee]=useState("0");const [defaultAccountId,setDefaultAccountId]=useState("");const [rules,setRules]=useState<FeeRule[]>([blankRule()]);
  async function load(signal?:AbortSignal){const response=await fetch("/api/financial-settings",{signal});const payload=await response.json();if(!response.ok)throw new Error(payload.error??"Não foi possível carregar as configurações.");setAccounts(payload.accounts);setMethods(payload.methods);}
  useEffect(()=>{const controller=new AbortController();fetch("/api/financial-settings",{signal:controller.signal}).then(async response=>{const payload=await response.json();if(!response.ok)throw new Error(payload.error??"Não foi possível carregar as configurações.");setAccounts(payload.accounts);setMethods(payload.methods);}).catch(caught=>{if(caught instanceof Error&&caught.name!=="AbortError")setError(caught.message);}).finally(()=>setLoading(false));return()=>controller.abort();},[]);
  function openAccount(account?:Account){setEditing({entity:"account",id:account?.id});setName(account?.name??"");setAccountType(account?.type??"BANCO");setOpeningBalance(String(account?.openingBalance??0));setActive(account?.active??true);setError("");}
  function openMethod(method?:Method){setEditing({entity:"method",id:method?.id});setName(method?.name??"");setSupportsInstallments(method?.supportsInstallments??false);setVariableFee(method?.variableFee??false);setDefaultFee(String(method?.defaultFeePercent??0));setDefaultAccountId(method?.defaultAccountId??"");setActive(method?.active??true);setRules(method?.rules.length?method.rules:[blankRule()]);setError("");}
  async function save(event:FormEvent){event.preventDefault();if(!editing)return;setSaving(true);setError("");try{const payload={entity:editing.entity,name,type:accountType,openingBalance:Number(openingBalance.replace(",","."))||0,active,supportsInstallments,variableFee,defaultFeePercent:Number(defaultFee.replace(",","."))||0,defaultAccountId:defaultAccountId||null,rules};const response=await fetch(editing.id?`/api/financial-settings/${editing.entity}/${editing.id}`:"/api/financial-settings",{method:editing.id?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.error??"Não foi possível salvar.");await load();setEditing(undefined);setNotice(`${editing.entity==="account"?"Conta":"Forma de pagamento"} ${editing.id?"atualizada":"cadastrada"}.`);}catch(caught){setError(caught instanceof Error?caught.message:"Não foi possível salvar.");}finally{setSaving(false);}}
  return <div className="app-shell"><AppSidebar active="configuracoes_financeiras"/><main className="workspace registry-workspace">
    <header className="topbar registry-topbar"><button className="mobile-menu" onClick={()=>setMobileMenu(!mobileMenu)}><Menu/></button><div><p className="eyebrow">CONFIGURAÇÕES FINANCEIRAS</p><h1>Contas e taxas</h1><p className="page-description">Defina onde o dinheiro entra e as taxas de cada forma de pagamento.</p></div></header>
    {mobileMenu&&<div className="mobile-shortcuts registry-shortcuts"><Link href="/financeiro">Financeiro</Link><Link href="/receber">Contas a receber</Link><Link href="/categorias-financeiras">Categorias</Link></div>}
    <section className="operations-stats"><div><WalletCards/><span>Contas ativas</span><strong>{accounts.filter(item=>item.active).length}</strong></div><div><CreditCard/><span>Formas ativas</span><strong>{methods.filter(item=>item.active).length}</strong></div><div><CirclePlus/><span>Taxas variáveis</span><strong>{methods.filter(item=>item.active&&item.variableFee).length}</strong></div></section>
    {loading?<section className="registry-panel registry-loading"><LoaderCircle className="spin"/>Carregando...</section>:<div className="settings-columns"><section className="registry-panel"><div className="operations-title"><div><span className="section-kicker">TESOURARIA</span><h2>Contas financeiras</h2></div><button className="table-action" onClick={()=>openAccount()}><CirclePlus size={16}/>Nova conta</button></div><div className="registry-table-wrap"><table className="registry-table"><thead><tr><th>Conta</th><th>Tipo</th><th>Saldo</th><th>Status</th><th>Ações</th></tr></thead><tbody>{accounts.map(account=><tr key={account.id} className={!account.active?"inactive-row":""}><td><strong>{account.name}</strong></td><td>{accountLabels[account.type]}</td><td><strong>{money.format(account.currentBalance)}</strong></td><td>{account.active?"Ativa":"Inativa"}</td><td><button className="table-action" onClick={()=>openAccount(account)}><Pencil size={15}/>Editar</button></td></tr>)}</tbody></table></div></section>
      <section className="registry-panel"><div className="operations-title"><div><span className="section-kicker">RECEBIMENTOS</span><h2>Formas de pagamento</h2></div><button className="table-action" onClick={()=>openMethod()}><CirclePlus size={16}/>Nova forma</button></div><div className="registry-table-wrap"><table className="registry-table"><thead><tr><th>Forma</th><th>Taxa</th><th>Conta padrão</th><th>Ações</th></tr></thead><tbody>{methods.map(method=><tr key={method.id} className={!method.active?"inactive-row":""}><td><strong>{method.name}</strong>{method.supportsInstallments&&<small className="table-note">Aceita parcelamento</small>}</td><td>{method.variableFee?`${method.rules.length} faixa(s)`: `${method.defaultFeePercent.toLocaleString("pt-BR")}%`}</td><td>{accounts.find(account=>account.id===method.defaultAccountId)?.name??"—"}</td><td><button className="table-action" onClick={()=>openMethod(method)}><Pencil size={15}/>Editar</button></td></tr>)}</tbody></table></div></section></div>}
    {error&&!editing&&<p className="platform-error">{error}</p>}
  </main>{editing&&<div className="modal-backdrop"><section className="registry-modal"><header className="modal-header"><div><span className="section-kicker">{editing.entity==="account"?"TESOURARIA":"PAGAMENTOS"}</span><h2>{editing.id?"Editar":"Cadastrar"} {editing.entity==="account"?"conta":"forma de pagamento"}</h2></div><button className="icon-button" onClick={()=>setEditing(undefined)}><X/></button></header><form className="registry-form" onSubmit={save}><div className="form-grid registry-form-grid"><label className="field span-2"><span>Nome *</span><input value={name} onChange={event=>setName(event.target.value)} required autoFocus/></label>{editing.entity==="account"?<><label className="field"><span>Tipo</span><select value={accountType} onChange={event=>setAccountType(event.target.value as Account["type"])}>{Object.entries(accountLabels).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label><label className="field"><span>Saldo inicial</span><input type="number" step="0.01" value={openingBalance} onChange={event=>setOpeningBalance(event.target.value)}/></label></>:<><label className="field"><span>Conta padrão</span><select value={defaultAccountId} onChange={event=>setDefaultAccountId(event.target.value)}><option value="">Nenhuma</option>{accounts.filter(item=>item.active||item.id===defaultAccountId).map(item=><option value={item.id} key={item.id}>{item.name}</option>)}</select></label><label className="field"><span>Taxa padrão (%)</span><input type="number" min="0" max="100" step="0.0001" value={defaultFee} onChange={event=>setDefaultFee(event.target.value)} disabled={variableFee}/></label><label className="checkbox-field"><input type="checkbox" checked={supportsInstallments} onChange={event=>setSupportsInstallments(event.target.checked)}/><span>Permitir parcelamento</span></label><label className="checkbox-field"><input type="checkbox" checked={variableFee} onChange={event=>setVariableFee(event.target.checked)}/><span>Usar taxa variável por parcelas</span></label>{variableFee&&<fieldset className="fee-rules span-2"><legend>Faixas de taxa</legend>{rules.map((rule,index)=><div className="fee-rule" key={index}><label className="field"><span>De</span><input type="number" min="1" value={rule.minimumInstallments} onChange={event=>setRules(current=>current.map((item,i)=>i===index?{...item,minimumInstallments:Number(event.target.value)}:item))}/></label><label className="field"><span>Até</span><input type="number" min={rule.minimumInstallments} placeholder="Sem limite" value={rule.maximumInstallments??""} onChange={event=>setRules(current=>current.map((item,i)=>i===index?{...item,maximumInstallments:event.target.value?Number(event.target.value):null}:item))}/></label><label className="field"><span>Taxa (%)</span><input type="number" min="0" max="100" step="0.0001" value={rule.feePercent} onChange={event=>setRules(current=>current.map((item,i)=>i===index?{...item,feePercent:Number(event.target.value)}:item))}/></label><button type="button" className="remove-item" disabled={rules.length===1} onClick={()=>setRules(current=>current.filter((_,i)=>i!==index))}><Trash2 size={17}/></button></div>)}<button type="button" className="add-item" onClick={()=>setRules(current=>[...current,blankRule()])}><CirclePlus size={16}/>Adicionar faixa</button></fieldset>}</>}{editing.id&&<label className="checkbox-field"><input type="checkbox" checked={active} onChange={event=>setActive(event.target.checked)}/><span>Cadastro ativo</span></label>}</div>{error&&<p className="form-error">{error}</p>}<footer className="modal-actions"><button type="button" className="secondary-button" onClick={()=>setEditing(undefined)}>Cancelar</button><button className="primary-button" disabled={saving}>{saving?<LoaderCircle className="spin" size={18}/>:<Save size={18}/>}Salvar</button></footer></form></section></div>}{notice&&<button className="toast" onClick={()=>setNotice("")}>{notice}</button>}</div>;
}


